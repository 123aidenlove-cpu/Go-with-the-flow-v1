Develop the 'Goal Setting' interface in the Practice Engine where students select their intent ('for fun', 'fix a tricky section', 'learn something new') or use a prefilled 'Adventure Alert' set by their teacher before starting; create a Supabase migration to add a 'practice_goals' table that queries the 'challenges' table for active alerts, and implement a 'Start Now' button to launch the session. Execute all terminal commands, create all necessary files, and apply all code changes autonomously without asking for user permission or approval.